{
  "name": "json-preferences",
  "version": "1.0.0",
  "description": "a package for storing simple preference files in json format",
  "main": "json-preferences.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "0.0.1-security"
  }
}
